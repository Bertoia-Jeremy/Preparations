{% form_theme form 'bootstrap_4_layout.html.twig' %}

<section class="container">
    {{ form_start(form) }}
    {{ form_widget(form) }}
        <button class="btn btn-primary my-2">{{ button_label|default('Sauvegarder') }}</button>
    {{ form_end(form) }}
</section>
