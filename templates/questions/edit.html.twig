{% extends 'base.html.twig' %}

{% block title %}Edit Question{% endblock %}

{% block body %}
    <div class="container my-2">
        <h1 class="text-center">Modifier une question</h1>
        <div class="w-100 text-end">
            <a class="btn btn-primary text-right" href="{{ path('questions_index') }}">Retour à la liste</a>
        </div>
    </div>

    {{ include('questions/_form.html.twig', {'button_label': 'Modifier'}) }}
{% endblock %}
